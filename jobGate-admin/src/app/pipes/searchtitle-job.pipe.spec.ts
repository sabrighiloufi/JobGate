import { SearchtitleJobPipe } from './searchtitle-job.pipe';

describe('SearchtitleJobPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchtitleJobPipe();
    expect(pipe).toBeTruthy();
  });
});
