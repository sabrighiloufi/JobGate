import { SearchnamecontractPipe } from './searchnamecontract.pipe';

describe('SearchnamecontractPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchnamecontractPipe();
    expect(pipe).toBeTruthy();
  });
});
