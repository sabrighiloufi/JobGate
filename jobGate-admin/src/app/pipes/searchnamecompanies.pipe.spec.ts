import { SearchnamecompaniesPipe } from './searchnamecompanies.pipe';

describe('SearchnamecompaniesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchnamecompaniesPipe();
    expect(pipe).toBeTruthy();
  });
});
