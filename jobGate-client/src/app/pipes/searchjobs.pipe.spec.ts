import { SearchjobsPipe } from './searchjobs.pipe';

describe('SearchjobsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchjobsPipe();
    expect(pipe).toBeTruthy();
  });
});
